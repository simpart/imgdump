/**
 * @file   header.c
 * @brief  bitmap header functions
 * @author simpart
 */
/*** include ***/
#include "igh/bitmap.h"

/*** function ***/
int ighbmp_gethdr (char *path, ighbmp_t *bmp) {
    if ((NULL == path) || (NULL == bmp)) {
        return IGH_NG;
    }
    // get bitmap file FILE pointer
    
    // 
    return IGH_OK;
}

/* end of file */
